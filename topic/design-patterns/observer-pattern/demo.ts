// import {extend, Subject, ObserverList} from './index';

// 引用dom元素
const controlCheckBox: HTMLButtonElement = document.getElementById(
  'mainCheckBox'
) as HTMLButtonElement;
const addBtn: HTMLInputElement = document.getElementById('mainCheckBox') as HTMLInputElement;
const container: HTMLDivElement = document.getElementById('observersContainer') as HTMLDivElement;
console.log(controlCheckBox, addBtn, container);
